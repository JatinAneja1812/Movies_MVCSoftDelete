@model IEnumerable<Movies.Web.Models.Nationality>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<h2>@ViewData["Nationaility"]</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.NationalityId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.NationalityId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.NationalityId">Delete</a> |
                    <a asp-action="People" asp-route-id="@item.NationalityId">People</a>
                </td>
            </tr>
        }
    </tbody>
</table>
